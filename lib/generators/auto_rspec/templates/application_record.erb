# frozen_string_literal: true
# todo remove it
# make include somewhere else and add it to description
require 'rails_helper'

RSpec.describe <%= @class_name %>, type: :model do
<% if @active_record_matchers[:relations].present? %>  describe 'relations' do
  <% for belongs_to_relations in @active_record_matchers[:relations][:belongs_to] -%>
  it { is_expected.to belong_to(:<%= belongs_to_relations %>) }
  <% end -%>
  <%- for belongs_to_relations in @active_record_matchers[:relations][:has_one] -%>
  it { is_expected.to have_one(:<%= belongs_to_relations %>) }
  <% end -%>
  <%- for belongs_to_relations in @active_record_matchers[:relations][:has_many] -%>
  it { is_expected.to have_many(:<%= belongs_to_relations %>) }
  <% end -%>
  <%- for belongs_to_relations in @active_record_matchers[:relations][:has_and_belongs_to_many] -%>
  it { is_expected.to have_and_belong_to_many(:<%= belongs_to_relations %>) }
  <% end -%>
end
<% end -%>
end